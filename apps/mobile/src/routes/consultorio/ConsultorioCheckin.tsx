export function ConsultorioCheckin() {
  return (
    <div style={{ padding: 24, maxWidth: 480, margin: '0 auto', backgroundColor: 'var(--bg-app)', minHeight: '100vh' }}>
      <h1 style={{ fontSize: 24, fontWeight: 700, letterSpacing: '-0.3px', color: 'var(--text-primary)', marginBottom: 8 }}>Check-in</h1>
      <p style={{ fontSize: 14, color: 'var(--text-secondary)', marginBottom: 24 }}>
        Consultório — Fluxo de check-in de pacientes presenciais.
      </p>

      <div className="card" style={{ padding: 32, textAlign: 'center' }}>
        <p style={{ fontSize: 16, color: 'var(--text-muted)' }}>
          Em desenvolvimento
        </p>
      </div>
    </div>
  );
}
